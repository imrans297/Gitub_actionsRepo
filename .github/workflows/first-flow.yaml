name: First workflow
on: [push]

jobs:
    run-shell-commands:
        runs-on: ubuntu-latest
        steps:
            - name: echo a stroing
              run: echo "Hello World"
            - name: MultiLines command
              run: 
                node -v
                npm -v
                   
