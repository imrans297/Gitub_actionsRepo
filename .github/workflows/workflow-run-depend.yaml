name: Workflow Run
on:
    workflow_run:
        workflow: [Repository Events]
        types: [completed]
jobs:
    echo-string:
        runs-on: ubuntu-latest
        steps:
            - run: echo "This Work_flow was triggered because 'Repository Events' was completed"        